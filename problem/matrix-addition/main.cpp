#include <iostream>
#include <vector>

std::vector<std::vector<int>> matrixAddition(std::vector<std::vector<int>> a, std::vector<std::vector<int>> b)
{
    int rows = a.size();
    int cols = a[0].size();
}

int main()
{
}